<div class="container  d-flex flex-column justify-content-center mt-2">
  <div class="col-12">
    <img src="{{rider.image}}" class="img-thumbnail" alt="{{rider.name}}" width="200" />
    <h1 class="mt-3">{{rider.name}}</h1>
    <img src="{{rider.flag}}" class="card-flag" alt="{{rider.name}}" width="20px" height="20px" />
    <p> <b>From</b>: {{rider.nationality}} </p>
    <p> <b>Bike:</b> {{rider.bike}} </p>
    <p> <b>Date Of Birth:</b> {{rider.dateOfBirth}} </p>
    <p> <b>Place Of Birth:</b> {{rider.placeOfBirth}} </p>
    <p> <b>Height:</b> {{rider.height}} </p>
    <p> <b>Story:</b> {{rider.story}} </p>
  </div>
  <p> <button>Merchadising</button></p>

  <a class="btn btn-secondary" href="/riders/{{rider._id}}/gallery">Gallery</a>
  
  <div>
    <a class="btn btn-success" href="/riders/{{rider._id}}/update" style="width: fit-content;"><i
        data-feather="edit"></i></a>
    <a class="btn btn-danger" href="/riders/{{rider._id}}/delete" style="width: fit-content;"><i
        data-feather="trash-2"></i></a>
    <a class="btn btn-secondary" href="/riders" style="width: fit-content;"><i data-feather="rider"></i></a>
  </div>
  <hr>
  <div class="col-12">
    <h2>Comments</h2>
    {{#each rider.comments}}
    <div class="card my-2">
      <div class="card-body">
        <p class="card-text">{{this.content}}</p>
      </div>
      <div class="card-footer d-flex justify-content-between">
        <small class="text-body-secondary">by {{this.user.username}}</small>
        <a href="/comments/{{this._id}}/delete" style="width: fit-content;"><i data-feather="trash-2" width="16"
            style="color: red;"></i></a>
      </div>

    </div>
    {{/each}}
    <form action="/comments/{{rider._id}}/create" method="POST">
      <div class="mb-3">
        <label for="comment" class="form-label">Comment</label>
        <textarea class="form-control" name="content" id="comment" rows="3" required></textarea>
      </div>
      <button type="submit" class="btn btn-success">Comment</button>
    </form>

  </div>