<div class="container my-5">
    <div class="row row-cols-1 row-cols-md-3 g-4">
        <section>
            {{#each arrGallery}}
            <img src={{this}}>
            {{/each}}
        </section>

    </div>
</div>